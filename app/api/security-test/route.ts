import {NextResponse} from "next/server";
import {compilePolicy,evaluate} from "../../../lib/policy";
export async function POST(req:Request){try{const b=await req.json();const policy=compilePolicy(String(b.policyText||""));const actual=evaluate(b.transaction,policy,Number(b.monthlySpend||0));const claimed=String(b.claimedDecision||"ALLOW");const tamperingDetected=claimed!==actual.decision;return NextResponse.json({success:true,attack:{claimedDecision:claimed,actualDecision:actual.decision,tamperingDetected},firewall:actual,razorpayCalled:false,message:tamperingDetected?"Frontend decision rejected. Server-side firewall remains authoritative.":"No decision tampering detected.",securityGuarantee:"Razorpay execution requires an independently evaluated server-side ALLOW or human-approved REVIEW."})}catch(e:any){return NextResponse.json({success:false,error:e?.message||"Security test failed."},{status:400})}}
